<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.illinois.lavanyap">

    <application
        android:allowBackup="true"
        android:icon="@drawable/main_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"></activity>
        <activity android:name=".Resource_Page" />
        <activity android:name=".User_Profile" />
        <activity android:name=".Ecotip" />

        <activity android:name=".User_projectpage" />

        <activity android:name=".List_of_Projects"/>
        <activity android:name=".Resource_projectpage"/>
        <activity android:name=".Login">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
         </activity>
        <activity
            android:name=".Projectcreate_page1"
            android:windowSoftInputMode="adjustPan"/>

    </application>

</manifest>